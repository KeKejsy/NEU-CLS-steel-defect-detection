# 依赖清单
# 主环境：D:\Miniconda3\envs\paddle_env  （conda 环境名 paddle_env，不是 env.paddle）
# 装包前先确认提示符是 (paddle_env)，不要用 base 环境
#
# ============ 已装好（2026-09-14 实测，不要重复装、不要升级）============
paddlepaddle==3.3.1         # CPU 版！无 CUDA。B 的分类训练就是用它在 CPU 上跑完的
numpy==2.5.2                # ★ 红线：不能降级。PaddleDetection 要求 <2.0，所以别装它
pillow==12.3.0              # 图像读写
matplotlib==3.11.1          # 出图
PyYAML==6.0.3               # B/C 都靠它读 yml 配置
tqdm==4.70.0                # 进度条
pandas==3.0.5
scikit-learn==1.9.0         # B 算混淆矩阵 / F1
scipy==1.18.1               # sklearn 的依赖

# ============ A / B / C 的代码实测都不需要，缺了也不影响运行 ============
# opencv-python             # 实测：B 和 C 的代码里没有任何 import cv2
# visualdl                  # 想看训练曲线可视化再装
# pycocotools               # 只在要用 COCO 指标时装（本项目用 VOC 口径），Python 3.12 可能编译失败
# gradio                    # 可选，C 做网页 Demo 时才装

# ============ 绝对不要装 ============
# paddledet                 # PyPI 上最新只有 2.6.0，要求 numpy<1.24，
#                           # 装了会强制降级 numpy 2.5.2，A/B/C 的代码一起崩。
#                           # C 实测过 git clone PaddleDetection release/2.9 也不可用
#                           # （import ppdet 逐层失败：缺 pycocotools → requests → imgaug…），
#                           # 最终 C 改用原生 Paddle 手写检测网络，零新增依赖。

# ============ 附：C 的机器（另一套环境，训练用，与主环境不同）============
# paddlepaddle-gpu==3.2.0   # RTX 5070 Ti Laptop 12GB
#                           # 路径 C:\Users\htt22\miniconda3\envs\paddle_env
